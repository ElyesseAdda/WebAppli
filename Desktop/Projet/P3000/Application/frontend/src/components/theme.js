import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgba(27, 120, 188, 1)",
    },
    secondary: {
      main: "#19857b",
    },
  },
});

export default theme;
